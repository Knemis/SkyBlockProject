package com.knemis.skyblock.skyblockcoreproject.utils;

import com.sk89q.worldguard.protection.flags.StateFlag;

public class CustomFlags {
    public static StateFlag VISITOR_SHOP_USE;
    // Potentially add a boolean like 'flagsRegistered = false;' to ensure registration happens once.
}
